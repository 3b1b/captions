import classes from "./Footer.module.css";

function Footer() {
  return (
    <footer>
      <div className={classes.summary}>5 edit(s)</div>
      <button>Submit</button>
    </footer>
  );
}

export default Footer;
